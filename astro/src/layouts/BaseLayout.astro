---
import Header from '../components/Header.astro'
import Footer from '../components/Footer.astro'
const { title } = Astro.props
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title ? `${title} — Prompt Loop` : 'Prompt Loop'}</title>
    <link rel="stylesheet" href="/prompt-loop-app/src/styles.css" />
  </head>
  <body>
    <main class="container">
      <Header title={title ?? 'Prompt Loop'} />
      <slot />
      <Footer />
    </main>
  </body>
</html>
